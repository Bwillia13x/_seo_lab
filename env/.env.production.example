## Production Environment (example) — do not commit secrets

# Public site configuration
NEXT_PUBLIC_SITE_BASE=https://seo.example.com
NEXT_PUBLIC_ALLOW_INDEXING=true

# AI / OpenAI configuration (set via secrets manager)
OPENAI_API_KEY=
OPENAI_DEFAULT_MODEL=gpt-5-mini-2025-08-07
AI_RATE_PER_MINUTE=30
AI_RATE_PER_DAY=100

# Optional: Upstash Redis for durable rate limiting
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Testing helpers (usually unset in prod)
UPDATE_SNAPSHOTS=false
PLAYWRIGHT_BASE_URL=https://seo.example.com

